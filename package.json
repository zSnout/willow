{
  "name": "@willow/willowjs",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -b && npm run bundle",
    "dev": "tsc -w",
    "serve": "esbuild --servedir=./dist",
    "minify": "esbuild src/**/* --outdir=dist --minify",
    "bundle": "esbuild src/index.ts --outfile=dist/willow.js --bundle --define:DEV=false --format=esm --minify"
  },
  "author": "",
  "license": "GPL-3.0",
  "devDependencies": {
    "csstype": "^3.1.1",
    "esbuild": "^0.15.7",
    "prettier": "^2.7.1",
    "typescript": "^4.8.3"
  }
}
