{
  "name": "@willow/willowjs",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -b && npm run bundle",
    "dev": "tsc -w",
    "serve": "esbuild --servedir=./dist",
    "minify": "esbuild src/**/* --outdir=dist --minify",
    "bundle": "esbuild src/index.ts --outfile=willow.js --bundle --minify --format=esm"
  },
  "author": "",
  "license": "GPL-3.0",
  "devDependencies": {
    "esbuild": "^0.15.7",
    "prettier": "^2.7.1",
    "typescript": "^4.8.3"
  }
}
